
class Main inherits IO {
    a: Int <- 10;
    b: String <- "Hello, World!";
    main() : Object { 
        {
            (new IO).out_string(b);
            (new IO).out_int(a);
            (new IO).out_int(b.length());
            (new IO).out_string(b.substr(0, 5));
            b <- b.concat("Hello");
            (new IO).out_string(b);
            
        }
    };
};