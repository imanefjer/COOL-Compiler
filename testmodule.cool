import Math;
class Main inherits IO{
    main() : Object {
        let a : Calculator in {
            out_int(a.add(1,3));
            if 1< 2 then out_string("\nModule is working ") else out_string("module is not working ") fi;

        }
    };
};